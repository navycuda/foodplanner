export enum AppActions{
  loadState   = "app.load.state",
  setView     = "app.set.view"
}

export enum CalendarActions{
  loadState   = "calendar.load.state",
  setDays     = "calendar.set.days"
}

export enum InventoryActions{
  loadState   = "inventory.load.state"
}